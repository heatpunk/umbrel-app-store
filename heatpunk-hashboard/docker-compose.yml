version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: heatpunk-hashboard_web_1
      APP_PORT: 80

  web:
    image: ghcr.io/heatpunk/hashboard:0.5.0
    restart: on-failure
    stop_grace_period: 1m
